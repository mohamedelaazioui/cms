<h1>Edit Service</h1>

<%= render 'form', service: @service %>

<div class="mt-3">
  <%= link_to 'Show', admin_service_path(@service), class: 'btn btn-info' %>
  <%= link_to 'Back', admin_services_path, class: 'btn btn-secondary' %>
</div>
