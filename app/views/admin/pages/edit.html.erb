<h1>Edit Page</h1>

<%= render 'form', page: @page %>

<div class="mt-3">
  <%= link_to 'Show', admin_page_path(@page), class: 'btn btn-info' %>
  <%= link_to 'Back', admin_pages_path, class: 'btn btn-secondary' %>
</div>
