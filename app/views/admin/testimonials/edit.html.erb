<h1>Edit Testimonial</h1>

<%= render 'form', testimonial: @testimonial %>

<div class="mt-3">
  <%= link_to 'Show', admin_testimonial_path(@testimonial), class: 'btn btn-info' %>
  <%= link_to 'Back', admin_testimonials_path, class: 'btn btn-secondary' %>
</div>
